{
  "name": "@tangible/design",
  "type": "module",
  "version": "2024.09.08",
  "scripts": {
    "start": "roll dev",
    "build": "roll build",
    "format": "roll format",
    "version": "node version.js",
    "wp": "concurrently --raw 'roll dev' 'wp-now start'"
  },
  "dependencies": { "@popperjs/core": "^2.11.8" },
  "devDependencies": {
    "@tangible/roller": "2.x",
    "@wp-now/wp-now": "^0.1.66",
    "concurrently": "^8.2.2"
  }
}
