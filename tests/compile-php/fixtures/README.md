# Compile-to-PHP Fixtures

Each fixture folder should contain:

- `template.ll.html`: template input for both runtime and compiled render.
- `context.json`: data context (if required by the template).
- `expected.html`: expected output for parity checks.
