{
  "name": "tangible-template-system-module",
  "scripts": {
    "--- Run commands below with name of module, like: npm run build admin": "",
    "--- Modules: admin, interface, logic, loop, template": "",
    "dev": "roll dev",
    "build": "roll build",
    "format": "roll format",
    "---": "",
    "build:all": "roll build admin; roll build interface; roll build logic; roll build template",
    "format:all": "roll format; roll format admin; roll format interface; roll format logic; roll format template"
  },
  "dependencies": {},
  "devDependencies": {
    "@tangible/roller": "1.x"
  }
}
