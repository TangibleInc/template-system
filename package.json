{
  "name": "tangible-template-system",
  "description": "Template System module",
  "homepage": "https://bitbucket.org/tangibleinc/template-system",
  "repository": "git@bitbucket.org:tangibleinc/template-system.git",
  "tangible": {
    "deployType": "module"
  },
  "scripts": {
    "--- Commands below can be run with module name, like: npm run build admin": "",
    "dev": "roll dev",
    "build": "roll build",
    "format": "roll format",
    "---": "",
    "build:all": "roll build admin; roll build interface; roll build logic; roll build template; roll build tester",
    "format:all": "roll format; roll format admin; roll format interface; roll format logic; roll format template; roll format tester"
  },
  "dependencies": {},
  "devDependencies": {
    "@tangible/roller": "1.x"
  }
}
